apiVersion: v1
kind: Service
metadata:
  name: elearn-service
  namespace: default
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  selector:
    app: elearn-app
